services:
  npc:
    build:
      context: .
      dockerfile: Dockerfile
    image: npc:1.0.2
    container_name: npc
    restart: always
    init: true
    network_mode: host
    volumes:
      - ./app:/app
    environment:
      TZ: Asia/Shanghai
    tty: true

networks: {}
